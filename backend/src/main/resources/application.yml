spring:
  application:
    name: moaspace

  datasource:
    url: ${DB_JDBC_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        # Batch Insert 설정 (성능 향상)
        jdbc:
          batch_size: 50
        order_inserts: true
        order_updates: true
    open-in-view: false

server:
  port: 8080

logging:
  level:
    me.muheun.moaspace: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

# 벡터 임베딩 설정 (DJL 기반 ONNX 모델)
vector:
  embedding:
    provider: onnx  # 임베딩 제공자 (onnx, openai, mock 등)
    model-path: models/models_minilm/model.onnx  # ONNX 모델 파일 경로 (상대 경로)
    tokenizer-path: models/models_minilm/tokenizer.json  # HuggingFace Tokenizer 파일 경로
    dimension: 384  # MiniLM-L12-v2 벡터 차원
    max-tokens: 512  # 최대 토큰 길이 (truncate)
