# Gradle JVM 설정
# E5-base 모델(1.0GB) 로딩을 위한 힙 메모리 증가
org.gradle.jvmargs=-Xmx3g -Xms2g -XX:MaxMetaspaceSize=512m

# 병렬 빌드 활성화
org.gradle.parallel=true
org.gradle.caching=true

# 데몬 활성화
org.gradle.daemon=true

# 테스트 실행 설정
# E5-base 모델 로딩을 위한 테스트 힙 메모리 증가
org.gradle.jvmargs=-Xmx3g -Xms2g
